
// Pointer Arithmatic is divided into four types
/*
1) Addition
2) subtraction
3) Multiplication
4) Division

*/

#include<iostream>


using namespace std;

/*
int main()
{
	int arr[4] = { 10,20,30,40 };// consider address of arr as 100

	int *p = arr;  // consider address of p as 200
	int *q = &(arr[3]);  // address of q as 300

	// Addition of pointer with number
	cout << *(p + 3) << endl; // 40
	// Addition of pointer with another pointer is not allowed
	// subtraction of pointer with number
	cout << *(q - 3) << endl;  // 10
	cout << p++ << endl; // 104
	cout << *(p++) << endl;  // 20
	// subtraction of pointer with another pointer is allowed if
	/*
	Note:
	1) both the pointer pointing to same memory location 
	2) if both the pointer of same datatype
	*/

/*
	cout << q - p << endl;  // 3	
	cout << q-- << endl;  // 108
	cout << *(q--) << endl;  // 30

	return 0;

}
*/

